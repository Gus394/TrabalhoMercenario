#include <iostream>
#include "fila.h"
#include "simulador.h"

int main()
{
    /*using namespace Estrutura;
    Fila<int> f;
    inicializar(f);
    queue(f, 10);
    queue(f, 10);
    queue(f, 10);
    queue(f, 4);
    dequeue(f);
    dequeue(f);
    dequeue(f);
    print(f);

    for (int i = 0; i < 256; i++) {
        std::cout << char(i) << " " << int(char(i)) << std::endl;
    }*/

    std::cout << char(1);
}
